<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ==================== PERMISSIONS ==================== -->

    <!-- Required for accessing camera hardware -->
    <!-- Used for: profile pictures, event photos, document scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Required for writing files to external storage (Android 10 and below) -->
    <!-- Used for: saving images, exporting files, caching data -->
    <!-- Note: For Android 11+, use Scoped Storage instead -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Required for reading files from external storage -->
    <!-- Used for: uploading images, accessing documents, reading cached files -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Required for network operations and FCM -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required to keep the device awake to receive FCM messages -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Required for posting notifications (Android 13/Tiramisu and above) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Required for vibration with notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Required to restart scheduled notifications after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Required for accessing precise location (GPS) -->
    <!-- Used for: recycling center locations, user location services -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Required for accessing approximate location (network-based) -->
    <!-- Used for: recycling center locations, user location services -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- 相机特性 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <queries>
        <provider android:authorities="com.facebook.katana.provider.PlatformProvider" />

        <!-- Required for opening web URLs (url_launcher) -->
        <!-- Used for: viewing emission data sources, opening external links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:label="fyp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- ==================== FCM CONFIGURATIONS ==================== -->
        <!-- Default notification channel for FCM -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="event_reminders" />

        <!-- Default notification icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />

        <!-- Default notification color (Android Lollipop+) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/primary" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Handle notification clicks -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- ==================== DEEP LINK CONFIGURATIONS ==================== -->
            <!-- Handle custom scheme deep links (smartwaste://event/12345) -->
            <!-- Used for: FCM notification clicks, external app links -->
            <!-- Note: Using 'smartwaste' as app scheme for better identification -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Custom scheme for deep links -->
                <!-- Format: smartwaste://event/{eventId} -->
                <data
                    android:scheme="saveearth"
                    android:host="event" />
            </intent-filter>

            <!-- ==================== ANDROID APP LINKS CONFIGURATIONS ==================== -->
            <!-- Note: Android App Links require a real domain and assetlinks.json file -->
            <!-- Since you don't have a domain, these are commented out for now -->
            <!-- You can uncomment and configure these when you get a real domain -->
            <!--
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="yourdomain.com"
                    android:pathPrefix="/event/" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="www.yourdomain.com"
                    android:pathPrefix="/event/" />
            </intent-filter>
            -->
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ==================== FACEBOOK CONFIGURATIONS ==================== -->
        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>
        <meta-data android:name="com.facebook.sdk.ClientToken" android:value="@string/facebook_client_token"/>

        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <!-- ==================== FCM SERVICE ==================== -->
        <!-- Firebase Cloud Messaging Service for handling background messages -->
        <!--        <service-->
        <!--            android:name=".MyFirebaseMessagingService"-->
        <!--            android:exported="false">-->
        <!--            <intent-filter>-->
        <!--                &lt;!&ndash; Listens for incoming FCM messages &ndash;&gt;-->
        <!--                <action android:name="com.google.firebase.MESSAGING_EVENT" />-->
        <!--            </intent-filter>-->
        <!--        </service>-->

        <!-- ==================== GOOGLE MAP SERVICE ==================== -->
        <!-- Google Maps API Key configuration -->
        <!-- Required for displaying Google Maps in the application -->
        <!-- Used for: recycling center locations, map navigation, location services -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDxxkSWPJGDFFpcejtULq6hHohdYuTLQ5A"/>

    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>